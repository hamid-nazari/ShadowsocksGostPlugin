<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:extractNativeLibs="true"
        android:installLocation="internalOnly">
        <activity
            android:name="com.github.shadowsocks.plugin.hnzgost.ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.shadowsocks.plugin.ACTION_CONFIGURE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="plugin"
                    android:host="com.github.shadowsocks"
                    android:path="/hnzgost"/>
            </intent-filter>
        </activity>

        <provider
            android:name="com.github.shadowsocks.plugin.hnzgost.BinaryProvider"
            android:authorities="com.github.shadowsocks.plugin.hnzgost.BinaryProvider"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.github.shadowsocks.plugin.ACTION_NATIVE_PLUGIN" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.github.shadowsocks.plugin.ACTION_NATIVE_PLUGIN" />

                <data
                    android:host="com.github.shadowsocks"
                    android:path="/hnzgost"
                    android:scheme="plugin" />
            </intent-filter>

            <meta-data
                android:name="com.github.shadowsocks.plugin.id"
                android:value="hnzgost" />
        </provider>
    </application>

</manifest>
